import { GeneralSettingsModel } from './general-settings-model';

describe('GeneralSettingsModel', () => {
  it('should create an instance', () => {
    expect(new GeneralSettingsModel()).toBeTruthy();
  });
});

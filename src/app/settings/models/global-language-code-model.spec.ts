import { GlobalLanguageCodeModel } from './global-language-code-model';

describe('GlobalLanguageCodeModel', () => {
  it('should create an instance', () => {
    expect(new GlobalLanguageCodeModel()).toBeTruthy();
  });
});

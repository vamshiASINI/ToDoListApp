import { GlobalCurrencyModel } from './global-currency-model';

describe('GlobalCurrencyModel', () => {
  it('should create an instance', () => {
    expect(new GlobalCurrencyModel()).toBeTruthy();
  });
});

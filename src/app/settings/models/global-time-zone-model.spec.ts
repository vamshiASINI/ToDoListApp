import { GlobalTimeZoneModel } from './global-time-zone-model';

describe('GlobalTimeZoneModel', () => {
  it('should create an instance', () => {
    expect(new GlobalTimeZoneModel()).toBeTruthy();
  });
});

import { ResGeneralSettingsModel } from './res-general-settings-model';

describe('ResGeneralSettingsModel', () => {
  it('should create an instance', () => {
    expect(new ResGeneralSettingsModel()).toBeTruthy();
  });
});

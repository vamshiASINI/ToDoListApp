import { GlobaldaysOfWeekModel } from './globaldays-of-week-model';

describe('GlobaldaysOfWeekModel', () => {
  it('should create an instance', () => {
    expect(new GlobaldaysOfWeekModel()).toBeTruthy();
  });
});

import { InstituteTypeGlobalModel } from './institute-type-global-model';

describe('InstituteTypeGlobalModel', () => {
  it('should create an instance', () => {
    expect(new InstituteTypeGlobalModel()).toBeTruthy();
  });
});

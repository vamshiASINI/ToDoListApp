import { GlobalCountryModel } from './global-country-model';

describe('GlobalCountryModel', () => {
  it('should create an instance', () => {
    expect(new GlobalCountryModel()).toBeTruthy();
  });
});

import { FacultyTypeResponseModel } from './faculty-type-response-model';

describe('FacultyTypeResponseModel', () => {
  it('should create an instance', () => {
    expect(new FacultyTypeResponseModel()).toBeTruthy();
  });
});

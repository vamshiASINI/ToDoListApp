import { BillAddressInstitution } from './bill-address-institution';

describe('BillAddressInstitution', () => {
  it('should create an instance', () => {
    expect(new BillAddressInstitution()).toBeTruthy();
  });
});

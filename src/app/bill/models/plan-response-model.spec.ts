import { PlanResponseModel } from './plan-response-model';

describe('PlanResponseModel', () => {
  it('should create an instance', () => {
    expect(new PlanResponseModel()).toBeTruthy();
  });
});

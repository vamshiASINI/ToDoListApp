import { BillPromoCodeModel } from './bill-promo-code-model';

describe('BillPromoCodeModel', () => {
  it('should create an instance', () => {
    expect(new BillPromoCodeModel()).toBeTruthy();
  });
});

import { PlanPriceResponseModel } from './plan-price-response-model';

describe('PlanPriceResponseModel', () => {
  it('should create an instance', () => {
    expect(new PlanPriceResponseModel()).toBeTruthy();
  });
});
